# 🚀 Инструкция по запуску проекта Baraka Ai

## Вариант 1: Локальный запуск (для разработки)

### 1. Создайте файл `.env` в корне проекта

Скопируйте `env.production.example` в `.env` и заполните значения:

```bash
cp env.production.example .env
```

Отредактируйте `.env` и укажите:
- `SECRET_KEY` - сгенерируйте: `python3 -c "import secrets; print(secrets.token_urlsafe(32))"`
- `POSTGRES_PASSWORD` - придумайте пароль для БД
- `OPENAI_API_KEY` - ваш ключ OpenAI (опционально, если используете AI функции)
- `TELEGRAM_BOT_TOKEN` - токен бота (опционально)

### 2. Установите и запустите PostgreSQL

```bash
# macOS (через Homebrew)
brew install postgresql@15
brew services start postgresql@15

# Создайте базу данных
createdb midas_db

# Примените схему
psql -d midas_db -f schema.sql
```

### 3. Установите зависимости бэкенда

```bash
# Установите зависимости Python
pip3 install -r requirements.txt

# Или используйте poetry (если установлен)
poetry install
```

### 4. Запустите бэкенд API

```bash
# Из корня проекта
uvicorn api.main:app --reload --host 0.0.0.0 --port 8000
```

API будет доступен на: http://localhost:8000
Документация: http://localhost:8000/docs

### 5. Установите зависимости фронтенда

```bash
# Перейдите в папку фронтенда
cd finance-tracker

# Установите зависимости
npm install
```

### 6. Запустите фронтенд

```bash
# Из папки finance-tracker
npm run dev
```

Фронтенд будет доступен на: http://localhost:5173 (или другой порт, который укажет Vite)

---

## Вариант 2: Запуск через Docker Compose

### 1. Установите Docker Desktop

Скачайте с https://www.docker.com/products/docker-desktop/

### 2. Создайте `.env` файл

```bash
cp env.production.example .env
# Отредактируйте .env и заполните все переменные
```

### 3. Запустите все сервисы

```bash
# Из корня проекта
docker compose up -d --build
```

### 4. Проверьте статус

```bash
docker compose ps
docker compose logs -f
```

### 5. Доступ к сервисам

- **API**: http://localhost:8001
- **Frontend**: http://localhost:3001
- **PostgreSQL**: localhost:5433

---

## Быстрый старт (только фронтенд для разработки)

Если нужно только запустить фронтенд и подключиться к существующему API:

```bash
cd finance-tracker
npm install
npm run dev
```

Убедитесь, что в `finance-tracker/.env` или `finance-tracker/vite.config.ts` указан правильный URL API.

---

## Полезные команды

### Остановить все контейнеры
```bash
docker compose down
```

### Пересобрать контейнеры
```bash
docker compose build --no-cache
docker compose up -d
```

### Просмотр логов
```bash
docker compose logs api -f    # Логи API
docker compose logs frontend -f  # Логи фронтенда
docker compose logs db -f     # Логи базы данных
```

### Запуск миграций (если используете Alembic)
```bash
docker compose exec api alembic upgrade head
# или локально
alembic upgrade head
```



